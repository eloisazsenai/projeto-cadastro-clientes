import { Routes } from '@angular/router';

/*
 * Aqui seriam declaradas as rotas (páginas) da aplicação.
 *
 * Por enquanto a lista está vazia porque o cadastro é exibido diretamente
 * pelo AppComponent. Um exemplo futuro seria associar o caminho "clientes" a
 * um componente específico.
 */
export const routes: Routes = [];
